

import java.io.IOException;
import java.io.PrintWriter;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

/**
 * Servlet implementation class HolaUsuario
 */
@WebServlet("/hola")
public class HolaUsuario extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    /**
     * @see HttpServlet#HttpServlet()
     */
    public HolaUsuario() {
        super();
        // TODO Auto-generated constructor stub
    }

	/**
	 * @see HttpServlet#doGet(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
		response.setCharacterEncoding("UTF-8");
		HttpSession sesion = request.getSession();  
		PrintWriter salida = response.getWriter();
		
		if (sesion.getAttribute("nombre") == null) {
			salida.append("Hola " + request.getParameter("nombre"));
			sesion.setAttribute("nombre", request.getParameter("nombre"));
		} 
		else {
			salida.append("Hola " + sesion.getAttribute("nombre"));
		}
		
	}

}
